module.exports = {
  presets: [
    'babel-preset-expo', // If you're using Expo
    '@babel/preset-react' // Add this to enable JSX transformation
  ],
  plugins: [
    // Add any plugins if necessary
  ]
};
